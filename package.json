{
  "name": "user-service-mock",
  "version": "1.0.0",
  "description": "Mock server pour Alumni Platform - User Service API",
  "scripts": {
    "start": "prism mock user-service.yaml -p 4010 -h 0.0.0.0 --cors -d",
    "start:static": "prism mock user-service.yaml -p 4010 -h 0.0.0.0 --cors"
  },
  "dependencies": {
    "@stoplight/prism-cli": "^5.8.4"
  },
  "engines": {
    "node": ">=18"
  }
}